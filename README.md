# stremio-api-tests

End-to-end integration tests for streaming media API endpoints.

## Setup

```bash
npm install
```

## Running Tests

```bash
npm test
```

## CI

Tests run automatically via GitHub Actions on every push to `main`.
